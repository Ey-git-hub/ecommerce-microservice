spring:
  # config:
  #   import: optional:file:${user.dir}/.env[.properties]
  datasource:
    url: jdbc:postgresql://ep-rough-scene-zamrnv5o-pooler.c-2.eu-west-2.aws.neon.tech/neondb?sslmode=require
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
#  SPRING_DATASOURCE_URL=jdbc:postgresql://ep-rough-scene-zamrnv5o-pooler.c-2.eu-west-2.aws.neon.tech/neondb?sslmode=require
#  SPRING_DATASOURCE_USERNAME=neondb_owner
#  SPRING_DATASOURCE_PASSWORD=npg_lKGV5vuQs1Sp

  # Enable automatic schema creation/updating
  jpa:
    hibernate:
      ddl-auto: update
      # generate-ddl: true

    # Useful for debugging: prints executed SQL statements to the console
    show-sql: true
server:
  port: 8081
